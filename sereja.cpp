#include <bits/stdc++.h>
int DP[105][105];
void solve(int* input,int n)
{

}

int main()
{
	int t,n,i;
	scanf("%d",&t);
	while(t--)
	{
		scanf("%d",&n);
		int input[n];
		for (i = 0; i < n; ++i)
		{
			scanf("%d",&input[i]);
		}
		memset(DP,0,sizeof(DP));
	}
	return 0;
}